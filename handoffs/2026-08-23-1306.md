# Handoff — 2026-08-23

## Project
`personal`

## Focus for next session
Review the 2 high-severity Dependabot alerts GitHub reports on `main`, then commit the
CLAUDE.md / CLAUDE.local.md updates from this session (they are still uncommitted).

## Context
Luis asked for Google Analytics on luissarmiento.com, configured the same way as the
Trifecta site. There was no GA property for this site — his Analytics account held only
Trifecta — so a new account, property and web stream were created through the browser,
and the tag was added to the Astro site, deployed and verified live.

## State
Done and deployed:
- GA4 account "Luis Sarmiento" (405640314) → property `luissarmiento.com` (551174257),
  stream "Personal website" (15485179603), measurement ID **G-N6C4L0GZLS**. Optional
  Google data-sharing off; ToS accepted under Switzerland with GDPR Data Processing
  Terms. "Internal Traffic" data filter set to Active.
- Tag renders from `BaseLayout` on all 36 pages. Commit `8bb8840`, merged to `main` as
  `a53c13f`, both pushed. Verified live on 6 routes with `age: 0`, and GA Realtime shows
  `page_view` / `session_start` / `first_visit` arriving.

Open:
- 2 high Dependabot alerts on `main` (new since 2026-07-25, when `npm audit` was 0).
- CLAUDE.md and CLAUDE.local.md edits from this session are uncommitted.
- Long-standing: decide whether to delete `.github/workflows/deploy.yml`.
- Cosmetic: the GA stream URL is the apex, canonical host is `www`.

## Key files / artifacts
- `src/components/Analytics.astro`
- `src/layouts/BaseLayout.astro`, `src/components/research/PublicationCard.astro`
- `CLAUDE.md` § Analytics, § Deployment
- Reference implementation: `/Users/lsarmiento/projects/Trifecta/src/components/Analytics.astro`

## Skills to load
None required. `/close` at the end of the next session.

## Open questions / watch-outs
- **The apex 301-redirects to `www.luissarmiento.com`.** `curl` without `-L` returns a
  45-byte redirect body, so a content grep finds nothing and looks exactly like a failed
  deploy. This cost ~7 minutes this session. Always `curl -sSL`.
- **An internal visit (`?internal=1`) will not appear in GA Realtime.** The filter is
  Active, i.e. it excludes rather than tags. That is correct behaviour, not a broken tag.
  To confirm collection, visit without the flag.
- `/tools/air-purifiers-trial/app/` is untagged on purpose — it is an iframe inside an
  already-tagged page. Do not "fix" it; that would double-count.
- Deploy is Netlify from `main`. A green GitHub Actions run says nothing about the live
  site — the Pages workflow deploys a site nobody visits.
- Headless/automation Chrome reports `visibilityState: "hidden"`, so `[data-animate]`
  elements stay at opacity 0 and pages look blank in screenshots. Artifact, not a bug.
