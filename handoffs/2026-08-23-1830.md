# Handoff — 2026-08-23

## Project
`personal`

## Focus for next session
Nothing is blocked or half-done. In about a week, open Search Console → Indexing → Pages
and check which of the 36 URLs Google actually indexed and which it declined, then act on
whatever it flags.

## Context
Luis asked whether GA had been configured last session; it had, but he then noticed it
had been set up under his CMCC work account rather than his personal Gmail. That was a
real single point of failure — the account had exactly one user, so CMCC deprovisioning
it would have orphaned the analytics for his personal site. Fixing it opened up the
broader question of what else was missing for understanding traffic, which turned out to
be: no Search Console at all, no sitemap, no robots.txt, canonical URLs pointing at a host
that redirects, three non-retroactive GA4 settings left at defaults, and no CI.

## State
All done and verified; nothing in progress.

- GA account 405640314: `gsaabogado@gmail.com` is the sole Administrator, cmcc.it removed.
- GA4 property config: event retention 14 months, `paper_title` custom dimension, key
  events `publication_click` / `cv_download` / `email_click`, timezone Switzerland.
- Repo: sitemap + robots.txt + `x-default` hreflang + `site` repointed to www
  (`aaafa53`); build-only CI workflow, green on first run (`1a865a9`).
- Search Console: domain property `sc-domain:luissarmiento.com` verified by DNS TXT,
  sitemap submitted (Success), linked to GA4.
- Only waiting item: Search Console and its GA4 reports are empty for a day or two while
  data accumulates. That is expected.

## Key files / artifacts
- `CLAUDE.md` §§ Analytics, SEO / crawlability, Google Search Console, Deployment
- `astro.config.mjs`, `public/robots.txt`, `src/layouts/BaseLayout.astro`
- `.github/workflows/build.yml`, `netlify.toml`
- Commits: `aaafa53`, `a058a86`, `ed01bfe`, `1a865a9`

## Skills to load
None required. `/close` at the end of the next session.

## Open questions / watch-outs
- **`luis.sarmiento@cmcc.it` is the browser's DEFAULT Google account.** Any bare Google
  link opens as work, where none of these properties exist. Force the right one with
  `?authuser=gsaabogado@gmail.com`. The account picker at top-left of GA switches
  PROPERTIES, not logins — clicking it jumps to the separate Trifecta account
  (404648778).
- **Do not delete the apex TXT record** in Netlify DNS. Search Console verification and
  the property go with it.
- **Node 22 is pinned in two places** — `.github/workflows/build.yml` and `netlify.toml`.
  If they drift, CI goes green while Netlify fails, which is exactly the 2026-07-25
  Astro 7 breakage.
- Screenshot coordinates go stale when the browser window resizes mid-task; re-screenshot
  before clicking. Never transcribe a token from a screenshot — the field truncates; use
  `read_page`.
- `netlify api createDnsRecord` returns 422 (cause unknown). Use the web DNS panel;
  read-only `getDnsZones` / `getDnsRecords` work fine.
