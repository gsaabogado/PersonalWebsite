# Handoff — 2026-08-29

## Project
`personal`

## Focus for next session
Open Search Console (Indexing → Pages) and look for URLs Google declined to index. The
sitemap went in on 2026-08-23, so a week of data should now exist. Nothing else is
outstanding on the site itself.

## Context
JEBO published "Emergency department visits and temperature: Evidence from Mexico" as
volume 250, article 107728, 2026, doi `10.1016/j.jebo.2026.107728`. The site and CV both
carried it as forthcoming since the 2026-08-10 acceptance, so this session promoted it to
a full citation everywhere it appears and pushed the result live.

## State
Done and verified live. Three commits, all on `main`, all pushed.

- `8e4cebb` (PersonalWebsite) — citation, DOI and "published" wording across
  `publications.ts`, `outreach.ts`, `HubContent.astro`, `tools.ts`, both hub index pages
  and the point-estimate README.
- `139b555` (cv) — `publications.csv` volume, pages, cleared note, corrected title.
- `93540e7` (PersonalWebsite) — refreshed `public/cv/Luis_Sarmiento_CV.pdf`.

All 19 CV variants re-rendered with `./render.sh all`. Verified by extracting text from
every PDF and the DOCX, not by exit code. Netlify deployed both pushes within ~30 s and
the live pages and live PDF were confirmed against content unique to the new build.

At 20:15, while this session was writing up, something OUTSIDE it committed and pushed
the unrelated in-progress work I had deliberately left alone — `6b511e7` (DevEdit overlay
plus `tools/dev-edit-plugin.mjs`) and `c168b5e` (the Corneta pitch page) — along with my
CLAUDE.md edit as `516ff53`. Those two are unreviewed and unverified here, and the Corneta
page is now live. Confirm that was intended.

## Key files / artifacts
- `~/projects/Personal/CLAUDE.md` — two new Key Patterns on where a citation lives and
  where the CV PDF comes from.
- `~/projects/Personal/CLAUDE.local.md` — this session in full.
- `~/projects/cv/CLAUDE.md` — two new invariants (private variants, render staleness).
- `~/projects/cv/CLAUDE.local.md` — the CV-side session entry.
- `~/Downloads/1-s2.0-S016726812600315X-main.pdf` — the publisher PDF.

## Skills to load
None required. `/close` when finished.

## Open questions / watch-outs
- **A paper's citation lives in more than one file.** `src/data/publications.ts` drives
  the publications page; `PAPER` in `src/data/outreach.ts` drives the ED-visits outreach
  hub, and status words also sit in `src/data/tools.ts` and both hub meta descriptions.
  Grep the journal name across `src/`, never just `publications.ts`.
- **A CSV commit in `~/projects/cv` is half-done until `./render.sh all` runs.** The
  2026-08-10 acceptance edit sat unrendered for 19 days and was only caught this session.
- **The 8 `_private` CV variants have no publications block at all.** A missing citation
  there is correct. Only 11 PDFs plus the DOCX carry publications.
- **Verify a deploy against something unique to the NEW build**, and always `curl -sSL` —
  the apex 301s to www, and a bare `curl` returns a 45-byte redirect body that greps as a
  failed deploy.
- There is no `personal_log.md` in the Obsidian `10-Status/` folder, so `last_active` was
  not updated; the handoff did archive to `11-Sessions/`.
- Another agent or hook is committing in this repo concurrently. Check `git log` before
  assuming an uncommitted file will stay uncommitted.
